import { renderCategoryPage } from './CategoryPageFactory.js';

export function renderBeautyPersonalCarePage(containerId, options) {
  renderCategoryPage(containerId, 'Beauty & Personal Care', options);
}
